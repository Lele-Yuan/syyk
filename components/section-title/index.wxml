<view class="sec-title">
  <view class="row">
    <view class="bar"></view>
    <view class="txt">{{title}}</view>
  </view>
  <view wx:if="{{extra}}" class="extra">{{extra}}</view>
  <view wx:elif="{{extraEn}}" class="extra-en">{{extraEn}}</view>
</view>
